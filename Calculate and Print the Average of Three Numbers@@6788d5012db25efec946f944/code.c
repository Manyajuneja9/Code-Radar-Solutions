#include <stdio.h>

int main() {
    float a,b,,c,d;
    int choice;
    printf("Enter three integers:");
    scanf("%f,%f,%f",&a,&b,&c);

    result=(a+b+c/3);
    printf("Average: %.2f\3",a,b,c);
    return 0;
}